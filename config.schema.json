{
  "pluginAlias": "SharkIQ",
  "pluginType": "platform",
  "singular": true,
  "customUi": true,
  "customUiPath": "./dist/homebridge-ui",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "default": "SharkIQ"
      },
      "oAuthCode": {
        "title": "SharkClean OAuth Code",
        "type": "string"
      },
      "vacuums": {
        "title": "SharkClean Vacuum Devices",
        "description": "The DSNs of the vacuums to add. Leave this empty to add every vacuum on your account (recommended unless you want to add only some of them).",
        "type": "array",
        "items": {
          "title": "SharkClean DSN",
          "type": "string"
        }
      },
      "europe": {
        "title": "SharkClean European Server",
        "description": "Set to true if you are located in Europe or the UK. SharkClean uses different servers for US and European regions.",
        "type": "boolean",
        "default": false
      },
      "invertDockedStatus": {
        "title": "Invert Vacuum Docked Status",
        "type": "boolean",
        "default": false
      },
      "dockedUpdateInterval": {
        "title": "Docked Update Interval",
        "type": "integer",
        "default": 30000,
        "minimum": 1000,
        "maximum": 2073600000
      },
      "externalAccessory": {
        "title": "Publish as External Accessory",
        "description": "When enabled, each vacuum is published as a standalone external accessory with its own HomeKit pairing code, instead of being bridged through Homebridge. Applies to both HAP and Matter registration modes.",
        "type": "boolean",
        "default": false
      },
      "preferMatter": {
        "title": "Prefer Matter",
        "description": "When enabled, the plugin will use the Homebridge Matter API (requires Homebridge v2.0+ with Matter enabled). Falls back to HAP if Matter is unavailable.",
        "type": "boolean",
        "default": true
      },
      "enableMatter": {
        "title": "Enable Matter",
        "description": "Allow the plugin to use Homebridge Matter support when available. Set to false to always use HAP accessories.",
        "type": "boolean",
        "default": true
      },
      "matrixClean": {
        "title": "Use Matrix Clean for room cleans",
        "type": "boolean",
        "default": false,
        "description": "When cleaning specific rooms from HomeKit, use the SharkClean app's \"Matrix Clean\" (a two-pass cross-hatch) instead of a plain single-pass clean. Only affects room cleans, not whole-house ones."
      },
      "errorSensor": {
        "title": "Show an Error sensor",
        "type": "boolean",
        "default": false,
        "description": "Adds a sensor that opens whenever the vacuum reports an error, such as a stuck brushroll or a missing dust bin, so you can be notified. Errors are always written to the Homebridge log whether or not this is switched on."
      },
      "waterTankSensor": {
        "title": "Show a Water Tank sensor",
        "type": "boolean",
        "default": false,
        "description": "Adds a sensor that opens when a fitted water tank has run dry, so you can be notified. A vacuum with no tank fitted stays closed. Only for models that mop."
      },
      "mopPlateSensor": {
        "title": "Show a Mop Plate sensor",
        "type": "boolean",
        "default": false,
        "description": "Adds a sensor that opens when the mop plate is not attached. Only for models that mop."
      }
    }
  },
  "layout": [
    {
      "type": "fieldset",
      "title": "SharkIQ OAuth Info",
      "expandable": true,
      "expanded": false,
      "items": [
        {
          "type": "help",
          "helpvalue": "<h5>OAuth Code</h5><em class='primary-text'>Recommended: use the Homebridge UI OAuth Assistant in the plugin Settings tab to generate the login URL and exchange callback code automatically. You can still paste the OAuth code here manually if needed.</em>"
        },
        "oAuthCode"
      ]
    },
    {
      "type": "fieldset",
      "title": "Vacuum Settings",
      "expandable": true,
      "expanded": true,
      "items": [
        {
          "type": "help",
          "helpvalue": "<h5>Vacuums</h5><em class='primary-text'>Enter SharkClean DSN(s) you want to add to HomeKit.</em>"
        },
        {
          "key": "vacuums",
          "notitle": true,
          "type": "tabarray",
          "title": "{{ 'New Vacuum' }}",
          "expandable": true,
          "expanded": false,
          "orderable": true,
          "items": [
            {
              "key": "vacuums[]",
              "title": "{{ value || 'New DSN' }}"
            }
          ]
        },
        "europe",
        "invertDockedStatus",
        "dockedUpdateInterval",
        "externalAccessory"
      ]
    },
    {
      "type": "fieldset",
      "title": "Matter Settings",
      "expandable": true,
      "expanded": false,
      "items": [
        {
          "type": "help",
          "helpvalue": "<h5>Homebridge Matter Support</h5><em class='primary-text'>These options require Homebridge v2.0 or later with Matter enabled. If Matter is unavailable the plugin will automatically fall back to standard HomeKit (HAP) accessories.</em>"
        },
        "preferMatter",
        "enableMatter",
        "matrixClean",
        "errorSensor",
        "waterTankSensor",
        "mopPlateSensor"
      ]
    }
  ]
}
